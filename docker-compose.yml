version: '3.8'

services:
  backend:
    build:
      context: ./backend  
    image: rajaramachandran/eventdesk:backend_uat_v1  
  frontend:
    build:
      context: ./frontend 
    image: rajaramachandran/eventdesk:frontend_uat_v1  